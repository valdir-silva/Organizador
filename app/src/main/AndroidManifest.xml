<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.alunando.organizador">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Organizador">
        <activity
            android:name=".DespesasActivity"
            android:exported="false"
            android:theme="@style/despesaTheme" />
        <activity
            android:name=".ReceitasActivity"
            android:exported="false"
            android:theme="@style/receitaTheme" />
        <activity
            android:name=".Principalctivity"
            android:label="@string/title_activity_principalctivity"
            android:theme="@style/Theme.Organizador.NoActionBar" />
        <activity android:name=".CadastroActivity" />
        <activity android:name=".LoginActivity" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.Intro">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>